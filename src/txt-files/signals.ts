/** 
    Why Signals?

    zone.js - third party library
    top to bottom change detection - quite expensive

    Signals - fine grained reactivity. 
    wrapper around a value which notifies when a value is changed.

    

    mySignal = signal<string>("Hello");

*/